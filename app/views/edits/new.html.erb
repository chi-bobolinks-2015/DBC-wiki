<%= form_for :edit, url: new_category_article_edit_path do |f| %>

<% if @edit.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@edit.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @edit.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>
 
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </p>
 
  <p>
    <%= f.submit "Create new article" %>
  </p>
 
<% end %>
 
<%= link_to 'Back', edits_path %>