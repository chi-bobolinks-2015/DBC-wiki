<%= form_for @edit do |f| %>

<% if @edit.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@edit.errors.count, "error") %> prohibited
        this edit from being saved:
      </h2>
      <ul>
        <% @edit.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>
 
 <%= hidden_field_tag(:category_id, @category.id) %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </p>
 
  <p>
    <%= f.submit "Create new Article" %>
  </p>
 
<% end %>
 
<%= link_to 'Back', category_path(:category_id) %>